import { ColorvrDirective } from './colorvr.directive';

describe('ColorvrDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorvrDirective();
    expect(directive).toBeTruthy();
  });
});
