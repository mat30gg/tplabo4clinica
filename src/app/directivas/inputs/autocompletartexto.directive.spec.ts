import { AutocompletartextoDirective } from './autocompletartexto.directive';

describe('AutocompletartextoDirective', () => {
  it('should create an instance', () => {
    const directive = new AutocompletartextoDirective();
    expect(directive).toBeTruthy();
  });
});
