import { TiempopipePipe } from './tiempopipe.pipe';

describe('TiempopipePipe', () => {
  it('create an instance', () => {
    const pipe = new TiempopipePipe();
    expect(pipe).toBeTruthy();
  });
});
