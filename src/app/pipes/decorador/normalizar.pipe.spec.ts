import { NormalizarPipe } from './normalizar.pipe';

describe('NormalizarPipe', () => {
  it('create an instance', () => {
    const pipe = new NormalizarPipe();
    expect(pipe).toBeTruthy();
  });
});
