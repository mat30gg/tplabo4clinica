import { BarrasivacioPipe } from './barrasivacio.pipe';

describe('BarrasivacioPipe', () => {
  it('create an instance', () => {
    const pipe = new BarrasivacioPipe();
    expect(pipe).toBeTruthy();
  });
});
